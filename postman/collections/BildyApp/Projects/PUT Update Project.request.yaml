$kind: http-request
name: PUT Update Project
method: PUT
url: '{{baseUrl}}/api/project/{{projectId}}'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "Updated Project"
    }
