$kind: http-request
name: GET Delivery Note PDF
method: GET
url: '{{baseUrl}}/api/deliverynote/pdf/{{deliveryNoteId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
